'use strict';

	class Point {
        constructor(x, y) {
            this.x = x;
            this.y = y;
        }
        toString() {
            console.log(this.x + ', ' + this.y);
        }
    }
	
export Point;